# AI智财助手：云开发数据库配置说明

本文档用于配置微信云开发数据库，适配当前小程序功能（首页、记账、分析、目标、学习）。

---

## 1. 前置条件

- 已在微信开发者工具开通云开发环境
- `miniprogram/app.js` 中 `env` 已填写正确环境 ID
- 已部署云函数 `cloudfunctions/quickstartFunctions`

---

## 2. 数据库集合

当前项目使用 2 个核心集合：

1. `finance_records`（账单记录）
2. `finance_goals`（目标记录）

> 代码会自动尝试创建集合，但建议你在控制台先确认集合已存在，便于检查权限和索引。

---

## 3. 字段设计

### 3.1 finance_records

每条账单字段建议如下：

- `openid`: `string`，用户标识（云函数内自动写入）
- `type`: `string`，`income` / `expense`
- `amount`: `number`，金额
- `category`: `string`，分类（如餐饮美食、交通出行）
- `note`: `string`，备注
- `date`: `number`，时间戳（毫秒）
- `createdAt`: `number`，创建时间戳（毫秒）

### 3.2 finance_goals

每个用户一条目标数据：

- `openid`: `string`
- `targetAmount`: `number`，目标金额
- `currentAmount`: `number`，当前已存
- `updatedAt`: `number`，更新时间戳

---

## 4. 权限配置（推荐）

在云开发控制台 -> 数据库 -> 集合权限里：

- 推荐使用 **“仅创建者可读写”** 或基于 `openid` 的规则
- 当前项目主要通过云函数读写，前端不直接写数据库

推荐规则（示意）：

- `finance_records`：仅文档创建者可读写
- `finance_goals`：仅文档创建者可读写

---

## 5. 索引配置（建议）

为提高查询性能，建议添加以下索引：

### finance_records

1. 复合索引：`openid`（升序） + `date`（降序）
2. 可选索引：`openid` + `type` + `date`

### finance_goals

1. 单字段索引：`openid`

---

## 6. 云函数环境变量（豆包）

如果你启用了豆包大模型能力（分析页一键建议、学习页重新测评）：

在云函数 `quickstartFunctions` 配置环境变量：

- `DOUBAO_API_KEY` = 你的 token（仅 token 本体，不含 `Bearer ` 前缀）

配置后需重新部署云函数。

---

## 7. 部署步骤（标准）

1. 打开微信开发者工具
2. 确认 `app.js` 中 `env` 正确
3. 右键 `cloudfunctions/quickstartFunctions`
4. 选择 **上传并部署：云端安装依赖**
5. 在云开发控制台确认集合、权限、索引、环境变量

---

## 8. 常见问题排查

### 8.1 报错 timeout

- 检查云环境 ID 是否正确
- 检查云函数是否部署成功
- 检查网络和云函数日志

### 8.2 数据写入成功但页面不更新

- 检查页面是否重新触发 `loadData()`
- 检查查询条件是否按 `openid` 过滤

### 8.3 豆包调用失败

- 检查 `DOUBAO_API_KEY` 是否配置
- 检查 token 是否过期
- 检查云函数日志返回内容

---

## 9. 备份建议（答辩/大创）

- 保留首页导出 CSV 功能，定期导出
- 关键演示前先准备一份 CSV 备份
- 如网络不稳定，可临时切本地模式演示

---

如果你需要，我可以再给你补一份：

- **《云函数部署与回滚手册》**
- **《数据库字段字典（可直接放进报告）》**
